// 1. Создаем файл, все как прошлых дз.

// Done

// 2. Почитать про теги: <label>, <form>, <input> 
// (источники прикреплены в конце ДЗ)

// Done

// 3. Сверстать данный footer, используя семантические теги 
// (footer, nav и т.д.)

// Done

// 4. К Форме, которая прикреплена в футере - добавить логику:
// email должен соответствовать стандартам (добавить валидацию), 
// если он не заполнен - форма не отправляется. 
// Кнопка "Подписаться" и есть "отправкой формы", 
// при нажатии на которую мы будем выводить 
// консоль лог в виде объекта:  { email: 'введенная почта' }

// Уровень 2:

// Немного теории:
// Модальное окно — это  всплывающее окно, 
// которое появляется поверх всей страницы и блокирует взаимодействие 
// с остальным интерфейсом, пока пользователь не выполнит в нём какое-то действие 
// (например, нажмёт кнопку, заполнит форму, закроет окно).

// 5. Создать кнопку "Регистрация". Создать модальное окно, 
// используя классы "modal, modal-showed". 
// Логика такая: при нажатии на кнопку у нас открывается модальное окно 
// путем добавления modal-showed к div с классом modal. 
// Не забываем добавить кнопку для закрытия модалки (крестик в углу).  
// Важные правила создания модалки:
// 1) Задний фон должен быть затемнён, но не полностью черный 
// (Создаем класс overlay, который будет затемнять всю страницу)
// 2) Модальное окно находиться ровно по центру страницы, независимо от масштаба

// const openButton = document.querySelector('#openModal');
// const closeButton = document.querySelector('#closeModal');
// const modal = document.querySelector('#modal');
// const overlay = document.querySelector('#overlay');

// Функция при открытии modal
// , (openButton.addEventListener('click') => {
//   modal.classList.add('modal-showed');
//   overlay.classList.add('modal-showed');
// })

// Функция при закрытии modal
// const closeModal = () => {
//   modal.classList.remove('modal-showed');
//   overlay.classList.remove('modal-showed');
// }

// Кнопка закрытия окна modal
// closeButton.addEventListener('click', closeModal);

// 6. Создать форму для регистрации внутри модального окна. 
// Она должна содержать поля: имя, фамилия, дата рождения, логин, пароль, повторение пароля. 
// Используйте <label> для того, что бы указать пользователю, какое поле за что отвечает. 
// Также важно использовать placeholder 
// (обо всем этом можно будет почитать в документации в конце поста) 
// Разрешается добавить поля на ваше усмотрение. 
// Все поля должны иметь валидацию. 
// Если пользователь ввел два разных пароля или форма невалидна (
// используем метод checkValidity()) - мы должны предупредить его о том, что регистрация отклонена. 
// Если регистрация успешна - выводим значения формы в лог, как в задании №4. 
// Дополнительно мы должны добавить к этому объекту свойство createdOn и указать туда время создания 
// (используем сущность new Date()). Также создайте внешнюю переменную user и присвойте ей этот объект. 
// После успешной регистрации - модалка должны закрыться. 

// let user = {};

// const userRegistrationForm = document.querySelector('#userRegistrationForm');
// const passwordInput = document.querySelector('#password');
// const confirmPasswordInput = document.querySelector('#confirmPassword');

// userRegistrationForm.addEventListener('submit', (event) => {
//   event.preventDefault();
//   const passwordsMatch = passwordInput.value === confirmPasswordInput.value;
//   if (!userRegistrationForm.checkValidity() || !passwordsMatch) {
//     alert('Регистрация отклонена: проверьте правильность данных или совпадение паролей!');
//     return;
//   }
//   const form = event.target;
//   const formData = new FormData(form);
//   const userData = Object.fromEntries(formData.entries());
//   userData.createdOn = new Date();
//   user = userData;
//   console.log('Новый пользователь:', user);
//   alert('Регистрация успешна!');
//   closeModal();
//   userRegistrationForm.reset();
// });

// 7. Добавить к пулл-реквесту видео с работой форм, 
// модального окна и сверстанного футера.

// Подсказка для тех, кому тяжело с созданием модального окна:
// Изначально мы создаем класс modal со всеми нужными стилями, 
// только ставим display: none по умолчанию. 
// modal-showed будет иметь внутри себя только 1 свойство - display: block. 
// При добавлении данного класса (через classList) мы будем отображать модальное окно. 
// P.S. - обязательно используйте свойство z-index для оверлея и модалки, 
// иначе они будут перекрывать друг друга.
